# Выполнение таблицы развития · 0.3.0

Код и инженерные проверки выполнены поверх существующего runner, контрактов и хранения. Новых npm-зависимостей нет. Получены оценки владельца трёх примеров; с идентичными повторами они покрывают шесть исходных диалогов. На подтверждённой ошибке завершён цикл предложения промпта и нового живого прогона. Команды и примеры: [WORKFLOWS.md](WORKFLOWS.md).

| Пункт исходной таблицы | Результат |
|---|---|
| Вход через разговор, повторное подключение | Успешное внешнее подключение запоминается; новый build без target использует его и показывает в плане. |
| HTTP, module, command, Python | Единые метаданные наблюдения, версии, расхода и промпта. Doctor выполняет проверяемые write/read/reset-запросы через настоящий адаптер. |
| Требования и источники | HTML показывает цитату и источник рядом с проверкой карточки. Clarify сохраняет вопросы/ответы и создаёт новый черновик с дополнительным материалом. |
| Редактирование карточек | Формы для отдельных проверок и рубрик без JSON; текстовый и семантический предпросмотр хорошего/плохого ответа. Версии, примеры, ссылки на события и расход сохраняются отдельно. |
| Golden cases и реальные диалоги | Локальный JSON/JSONL-импорт через CLI и Pi, валидация существующими схемами. Происхождение назначается путём импорта. |
| Общие профили | Существующий редактор сохраняет исходный профиль, показывает связанные карточки и применяет override; поведение сохранено и проверено. |
| Static, scripted, reactive | Существующие режимы сохранены. Добавлена сводка человечески подтверждённых находок на сопоставимых попытках и фактический пилот. |
| Точные проверки | Нельзя засчитать копию initialState без наблюдения, отсутствующее поле, неподтверждённый reset, неполную трассу или инструмент за пределами scope. |
| Песочница и внешнее состояние | Детерминированный и настоящий LLM-адаптеры с транзакциями SQLite и новой БД на диалог. LLM-адаптер подтвердил историю, сброс, полную трассу, версию, promptHash и расход. AIGW-адаптер имеет более узкую область наблюдения. |
| Модельный судья | Переоценка сохранённых трасс в отдельный результат; старые оценки и факты не меняются. Проверка цитат с ограниченными попытками исправления, учёт границ наблюдения и фактически доставленных реплик. Сравнение калибровки по исходным человеческим меткам неизменных критериев. |
| Ручной разбор | «Ошибся агент / ошибся тест / данных недостаточно / задача выполнена», точный trial и заметка; история вердиктов сохраняется. Время чтения выбранного диалога и формы, включая простой; быстрый вердикт тоже учитывается. |
| Повтор и сравнение версий | Фиксированные карточки, парные сравнения, release адаптера и evaluatorVersion. Можно переоценить обе связанные версии одним судьёй и сравнить их. Одиночная переоценка и разные условия не дают заявления об исправлении. |
| Улучшатор | Sandbox учитывает семантические провалы и полноту оценивания. Для внешнего агента: один promptFile, подтверждённые dev-ошибки, гипотеза, проверяемый diff, отдельный кандидат, неизменный набор тестов. |
| Сохранённая регрессия и CI | Относительные пути, override до preflight, listing, исходные run/trial/вердикты в suite. Готовый набор запускается также основным [.github/workflows/check.yml](../.github/workflows/check.yml); доказательства загружаются даже при ошибке. |
| Отчёты | Текущий автономный HTML расширен происхождением, версиями, наблюдениями, раздельными расходами и старыми/новыми оценками; JSON и Markdown содержат те же данные. |
| Статистика | Отделены ошибки исполнения, исключённые попытки, неполные оценки, ошибки симулятора и подтверждённые находки; неизвестная стоимость остаётся неизвестной. |
| Исполнение и хранение | Сохранены единый последовательный runner, лимиты, отмена, один писатель, checkpoints и журнал. Переоценка использует те же ограничения и хранение. |
| Офлайн-демо | Сохранено; добавлен детерминированный пример полного цикла с SQLite. Не выдаётся за доказательство качества LLM. |

## Шесть шагов плана: приёмка

1. **Достоверность.** Все три воспроизведения из обзора перенесены в обычные регрессионные тесты. Дополнительно закрыта подмена происхождения через редактор, проверяются пропавший последний snapshot, scope инструментов и смена версии/сессии.
2. **Повтор без настройки.** Тест переносит suite, подставляет действующее подключение вместо несуществующего пути и исполняет его CLI. Исходные доказательства сохранены, новые человеческие вердикты не придуманы.
3. **Проверка оценщика.** На живой модели переоценены все девять исходных и девять исправленных диалогов без повторного обращения к агенту. Исходные записи сохранены. Новая рубрика отдельно проверена на хорошем и плохом ответах: pass/fail совпали с примерами. Полученные человеческие метки выявили ограничение: обе версии судьи согласны с владельцем в 4/6 размеченных диалогов; обе пропустили два идентичных нарушения строгого формата. Это измеренная ошибка модельного судьи, не повод переписывать исходные оценки. Точные проверки оба провала обнаружили.
4. **Адаптер и состояние.** Doctor прошёл на настоящем LLM-агенте с двумя отдельными SQLite-базами: три запроса, история, сброс, сохранение других полей, полная трасса зарегистрированных инструментов, версия кода/модели, хеш промпта и расход. AIGW по-прежнему наблюдает только разговор и IDP/SBE: его бизнес-системы не сбрасывались и его расход неизвестен.
5. **Польза реактивного режима.** Выполнены пилоты AIGW и SQLite LLM. В последнем — три неизменные карточки × три режима; расходы измерены. Владелец оценил три примера и их идентичные повторы: шесть диалогов, из них три pass и три fail по строгому синтетическому протоколу. Остальные три не размечены; время разбора через чат не измерялось. Дополнительная польза reactive не подтверждена имеющимися человеческими оценками.
6. **Ограниченный улучшатор.** На настоящем LLM проверены две изолированные правки одного промпта тем же набором: точные проверки 5/9 → 6/9 → 9/9. После получения оценки владельца полный путь `proposePrompt → inspectPrompt → promptVersion → start` повторён на подтверждённой dev-ошибке с подставным ID: новый запуск снова прошёл 9/9 точных проверок и агентских рубрик. Исходный файл сохранён. Diff проверен coding-агентом в рамках поручения завершить работу; отдельное ручное одобрение diff владельцем не заявляется. Запреты на неподтверждённые ошибки, подмену diff и использование control проверены интеграционными тестами.

## Дополнительная инженерная приёмка · SQLite LLM

Испытуемый, симулятор и судья использовали настоящие вызовы OpenRouter / Claude Haiku 4.5. Исходный промпт содержал неоднозначный шаблон: агент печатал `ID: 11:00.` либо добавлял пояснение к точному протокольному ответу. Исправленный промпт задаёт формат полного ответа и примеры. Это учебный протокол записей, не бизнес-политика AIGW.

| Этап | Прогон | Точные проверки | Расход лаборатории / испытуемого, USD |
|---|---|---|---|
| Исходный | `a7415ae0-d108-40f9-90df-e11f4b4eeb87` | 5/9 | 0,041755 / 0,032342 |
| Первый кандидат | `57e1041d-95d8-48b1-8932-387087ff7429` | 6/9 | 0,042421 / 0,034635 |
| Второй кандидат | `c5dcbe44-864b-40d5-9a56-a0d2a3a337c5` | 9/9 | 0,036974 / 0,042073 |
| Повтор через prompt-propose после человеческой оценки | `fb9af058-62d4-4c60-becb-91152538ffa8` | 9/9 | 0,030608 / 0,042018 |

Оба исходных набора переоценены одной финальной версией судьи: `10b76b16-819b-4023-92ff-db52b2701c03` и `22015a00-5337-4bde-8599-0aa6adbeb12b`. Сопоставлены 9/9 пар; совокупная оценка выросла в пяти диалогах, регрессий нет. После правки все девять агентских рубрик — pass. Это предварительное измерение на трёх синтетических карточках, не доказательство качества в продакшне.

Doctor: два валидных диалога, шесть модельных вызовов испытуемого, $0,010029. Предпросмотр новой рубрики: два модельных вызова, $0,004437; хороший ответ принят, плохой отклонён. Промежуточные сбои провайдера и ошибочные оценки сохранены в отдельных запусках; расход неполных вызовов не объявляется известным.

Материалы находятся в `.context/completion/`: `comparison.json`, `report.html`, `doctor-verified.json`, `preview-result.json`, исходные записи в `runs/`, запись терминальной проверки `ui-smoke.txt`. Исправленный [llm-prompt.md](../examples/llm-prompt.md) и [адаптер](../examples/llm-stateful-agent.mjs) входят в пакет.

## Приёмка владельца и завершение цикла

Владелец подтвердил: корректный перенос — задача выполнена; подставной `ID` — ошибка агента. Для ответа с пояснением перед `Which record?` сохранена оговорка: это fail при явно заданном строгом протоколе, а в обычном диалоге неверным было бы ожидание теста. В гипотезу предложения промпта включён только однозначно подтверждённый случай с `ID`.

В исходную запись добавлены 12 аннотаций: по шести диалогам и их рубрике `contract`. Это шесть оценённых диалогов трёх типов, не 12 независимых оценок. Переоценки не получили вымышленных новых человеческих меток. Калибровка: 3 верно принятых ответа, 1 верно найденный провал, 2 пропущенных провала; согласие 4/6 и полнота обнаружения провалов 1/3. Эта маленькая выборка с повторами не подтверждает надёжность судьи.

Финальный кандидат переоценён отдельно: `8676481b-3b1d-44df-8e2e-cde5a5fac5ed`, 9 вызовов судьи, $0,029171, без вызова испытуемого. С исходной переоценкой сопоставлены 9/9 пар: улучшились пять диалогов, регрессий нет; две из трёх карточек улучшились. Артефакты: `.context/completion/human-verdicts.json`, `human-prompt-proposal.json`, `human-prompt-run.json`, `human-prompt-report.html`. Это повторная проверка на известных dev-кейсах, не независимое подтверждение обобщения.

## Живой пилот 2026-09-13

Проверенный прогон: `55edf934-8e07-42d1-8525-d1b4d5cc8247`. AIGW работал на локальном сервисе с тестовыми IDP/SBE-материалами. Лабораторные роли использовали настроенный OpenRouter / Claude Haiku 4.5. Все шесть диалогов завершились и получили оценки по рубрикам. Судья отметил недостатки ответа во всех шести и поведение пользователя в двух. Это предварительные модельные оценки, не шесть подтверждённых бизнес-дефектов.

Наблюдаемый расход лаборатории: 10 вызовов, 27 314 входных и 4 358 выходных токенов, **$0,05057375**. Сумма включает последующую группировку ошибок; суммы по режимам содержат только расходы внутри диалогов. Стоимость модели AIGW неизвестна. Времени человеческого разбора нет.

Переоценка: `72884bf3-12e5-4c29-8219-f647e7f92787`, один сохранённый диалог, один вызов судьи, **$0,005472**, без повторного вызова агента. Исходная оценка сохранена; новая также fail.

Первый запуск `0b380722-3e2f-45cc-bd6e-8a09a0681cf2` сохранён как неполное измерение: выбранная модель OpenAI Codex была перечислена в настройках, но реальные запросы не прошли. Доступ другого настроенного провайдера проверен коротким запросом, после чего выполнен отдельный полный прогон. Неудачные попытки не удалены и не подмешаны к успешной выборке.

Локальные JSON/JSONL и отчёты находятся в `.context/live-pilot/`; они не входят в распространяемый пакет. Пилот не использует банковские политики, утверждённые владельцем, или логи настоящих клиентов.

## Проверки и границы

- `npm test`: **190 passed**, без пропусков; в том числе реальные локальные HTTP/Node/Python-процессы, SQLite, CLI, загрузчик Pi SDK, семантический предпросмотр, калибровка, ссылки на события и сравнение переоценённых версий.
- `npm run typecheck` — успешно, включая расширения Pi. `npm pack --dry-run --json` — успешно, 85 файлов, новые команды/адаптеры/документы включены.
- Точный пример CI выполнен локально: **exit 0, 2/2 диалога пройдены**. GitHub workflow не запускался и пакет не публиковался.
- Настоящий интерфейс Pi пройден в терминале: поля рубрики → сохранение → запуск демо → просмотр диалога → форма разбора. Автотесты дополнительно проходят семантический предпросмотр и учёт времени чтения.
- HTML проверен на экранирование, CSP и ссылки; в финальном живом отчёте 131 уникальный ID и 175 корректных внутренних ссылок. Визуальный просмотр через браузер не выполнен: политика браузера заблокировала открытие локального файла.
- Очереди, серверный веб-интерфейс, командные аккаунты и универсальные коннекторы к логам не добавлены: исходная таблица предлагала сохранять существующий локальный подход до конкретной потребности.
