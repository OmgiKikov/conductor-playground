{
  "name": "pi-agent-lab",
  "version": "0.3.0",
  "description": "Check an agent change from a conversation: reproducible tests, evidence and regression comparisons",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "bin": {
    "agent-lab": "dist/cli.js"
  },
  "files": [
    "dist",
    "extensions",
    "skills",
    "docs",
    "examples",
    "README.md",
    "CONTEXT.md"
  ],
  "pi": {
    "extensions": [
      "./extensions/agent-lab.ts"
    ],
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "typecheck": "npm run build && tsc --noEmit --strict --noUncheckedIndexedAccess --target ES2023 --module NodeNext --moduleResolution NodeNext --allowImportingTsExtensions --skipLibCheck extensions/*.ts",
    "demo": "tsx src/cli.ts demo",
    "start": "npm run build && node dist/cli.js chat",
    "test": "npm run build && tsx --test test/*.test.ts",
    "pi": "pi -e ./extensions/agent-lab.ts --skill ./skills/agent-builder/SKILL.md",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@earendil-works/pi-tui": "0.85.1",
    "typebox": "1.3.7",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.3"
  }
}
